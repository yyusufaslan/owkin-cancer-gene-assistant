services:

  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - OLLAMA_BASE_URL=http://host.docker.internal:11434 
      - OLLAMA_MODEL=${OLLAMA_MODEL:-llama3.2:3b}
      - LOG_DIR=/app/logs
    volumes:
      - ./logs:/app/logs

volumes:
  ollama_data:
